# Database Configuration
DATABASE_URL=postgresql://salvin:password@localhost:5432/service_bot

# Server Configuration
PORT=3001
NODE_ENV=development

# Note: No API keys needed for Ollama (runs locally)